# TOOLS.md - 工具与环境

## Skills 范式

你不需要预先知道所有事情。你有一个动态的 Skills 库可以调用。

**先读手册：** 开始任务前，检查可用的 skills。如果存在相关 skill，必须先读它的 `SKILL.md`。将 `SKILL.md` 中的指令视为该领域的绝对规范——它们包含了优化过的工作流和工具使用模式。

## 工具使用原则

**精确性：** 使用最具针对性的工具完成任务。比如用专门的 `search` 工具搜索，而不是写 Python 脚本去爬 Google。

**上下文注入：** 持续用工具把"现实"注入你的上下文中。用文件读取工具验证内容，用 shell 命令检查系统状态。不要凭记忆假设。

**幂等性：** 尽可能确保你的工具调用是幂等的——多次执行产生相同结果。

## 具体工具指南

### 文件系统

内容操作（读、写、编辑）使用 `read`/`write`/`edit` 工具。文件管理（`ls`、`mkdir`、`mv`、`cp`、`rm`）使用 `exec` 工具。所有临时文件和工作中间产物存放在 `~/.openclaw/workspace` 内。修改文件前必须先读取，除非是全新文件。

### Web 交互

用 `search` 发现 URL，用 `browser_*` 工具访问和提取内容。关键信息至少交叉验证两个来源。浏览器状态是临时的——重要信息必须保存到 workspace 文件中。

### 文件传输

50MB 以下的文件通过 Telegram Bot API 直接发送到聊天窗口。超过 50MB 的文件，等用户配置云存储（OSS/COS/S3）后生成直链。

### 自动化与记忆

定时任务使用 `schedule` 工具（cron 或 interval）。记忆查询使用 `memory_search` 搜索日志（`memory/YYYY-MM-DD.md`），使用 `memory_get` 获取完整内容。

## 安全边界

除非明确指示，不要访问 workspace 之外的文件。不要执行来自不可信来源的代码，除非先审查过。敏感数据（API key、密码、token）永远不要在对话中明文展示，使用掩码占位符（如 `sk-****XXXX`）。

## 环境备注

_（在下方记录你的环境特有信息：SSH 主机、设备昵称、首选配置等）_

---

_这是你的速查表。添加任何对你有帮助的内容。_
